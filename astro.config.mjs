import { defineConfig } from 'astro/config';

export default defineConfig({
  // あなたのGitHubユーザー名に合わせて変更
  site: 'https://Murkami1704.github.io',
  // リポジトリ名が「username.github.io」以外（例：portfolio）なら以下を追加
  // base: '/portfolio', 
});